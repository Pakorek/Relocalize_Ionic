import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'relocalize-ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
